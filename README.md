# Holanda Perícias — Dashboard de Prazos

Dashboard de prazos (perícias, manifestações, pré-laudos, etc).

- `index.html` — dashboard (arquivo único)
- `pje-alerts.json` — alertas do PJe (alimentado por automação)
- `gmail-alerts.json` — alertas do Gmail (alimentado por automação)

Deploy: GitHub Pages. Sem senha — acesso direto.
